{
  "name": "2021-portfolio-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "js-cookie": "^2.2.1",
    "node-sass": "^4.14.1",
    "react": "^16.3.0",
    "react-dom": "^16.3.0",
    "react-dom-confetti": "^0.2.0",
    "react-particles-js": "^3.4.1",
    "react-router": "^5.2.0",
    "react-router-dom": "^5.2.0",
    "react-scripts": "4.0.1",
    "react-spring": "^8.0.27",
    "react-string-replace": "^0.4.4",
    "react-typed": "^1.2.0",
    "web-vitals": "^0.2.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "aws s3 sync build/ s3://$AWS_S3_BUCKET --profile jordan --debug && aws cloudfront create-invalidation --distribution-id $JORDAN_CLOUDFRONT_DISTRIBUTION_ID --paths \"/*\" --profile jordan --debug",
    "invalidate-cache": "aws cloudfront create-invalidation --distribution-id $JORDAN_CLOUDFRONT_DISTRIBUTION_ID --paths \"/*\" --profile jordan --debug",
    "lint": "eslint --ext js --ext jsx src"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "sass-loader": "^8.0.2"
  }
}
